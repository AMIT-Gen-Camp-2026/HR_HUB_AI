# notebooks/

Exploration only.

**Nothing here is imported by `app/`.** A notebook is where you discover something; `app/`
is where you commit to it. If a notebook produces something the service needs, move the
code into `app/` with a test.

Clear outputs before committing — `nbstripout` is in the pre-commit hooks.
