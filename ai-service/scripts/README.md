# Scripts

One-off and operational scripts. Anything that runs regularly belongs in `app/` or `eval/`.

| Script | Purpose |
|---|---|
| `make_fixture_cv.py` | Generate a synthetic CV for tests |
| `check_env.py` | Verify the selected provider is reachable before you waste a morning |
