# data/

Local scratch space — never commit real applicant presentations here (.gitignore
already excludes data/*.pptx and data/cache/). Put small synthetic test files under
tests/fixtures/ instead; this folder is for eval datasets and local caching only.
